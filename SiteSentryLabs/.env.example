# Local Express server only (the live site uses Cloudflare Pages Functions).
# Generate one: node -e "console.log(require('crypto').randomBytes(24).toString('base64url'))"
ADMIN_PASSWORD=
PORT=3000

# Optional: OpenAI-compatible provider for Sohana (falls back to local replies if unset)
AI_API_URL=
AI_API_KEY=
AI_MODEL=
